{/* 
    cookies 
    -> on browser (CONVINANCE)
    while cookies are smaller pieces of data stored on the client-side (user's browser) and can persist across multiple visits. 

    session
    -> on server (SECURITY SBI WEBSITE)
    Sessions are primarily server-side, managing user interactions and states during a single visit
    */}